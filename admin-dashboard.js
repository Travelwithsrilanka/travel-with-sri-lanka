import { createClient } from "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm"; // ========================================================= // SUPABASE // ========================================================= const SUPABASE_URL = "https://vbbmnzqrvoceqbwwwsrc.supabase.co"; const SUPABASE_PUBLISHABLE_KEY = "sb_publishable_HZ1A8CURkRFs0v21FUT0VA_44dtPzr3"; const supabase = createClient( SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, { auth: { persistSession: true, autoRefreshToken: true, detectSessionInUrl: true } } ); // ========================================================= // SHOW ERROR // ========================================================= function showFatalError(message) { document.body.innerHTML = ` <div style=" min-height:100vh; display:flex; align-items:center; justify-content:center; padding:20px; background:#f4f7f5; font-family:Arial,Helvetica,sans-serif; "> <div style=" width:100%; max-width:600px; background:white; padding:30px; border-radius:16px; box-shadow:0 5px 25px rgba(0,0,0,.08); "> <h2 style="margin-top:0;color:#c0392b;"> Admin Panel Error </h2> <p style=" line-height:1.7; color:#555; word-break:break-word; "> ${escapeHTML(message)} </p> <button onclick="location.href='admin.html'" style=" border:0; background:#176b4d; color:white; padding:12px 20px; border-radius:8px; cursor:pointer; font-size:16px; " > Back to Login </button> </div> </div> `; } // ========================================================= // HTML ESCAPE // ========================================================= function escapeHTML(value) { if (value === null || value === undefined) { return ""; } return String(value) .replace(/&/g, "&amp;") .replace(/</g, "&lt;") .replace(/>/g, "&gt;") .replace(/"/g, "&quot;") .replace(/'/g, "&#039;"); } // ========================================================= // LOGIN CHECK // ========================================================= async function checkLogin() { try { console.log("Checking Supabase session..."); const result = await Promise.race([ supabase.auth.getSession(), new Promise((_, reject) => { setTimeout(() => { reject( new Error( "Supabase session check timed out." ) ); }, 10000); }) ]); const { data, error } = result; console.log("Session result:", data); if (error) { console.error("Session error:", error); showFatalError( "Session error: " + error.message ); return false; } if (!data || !data.session) { console.log("No active session."); window.location.href = "admin.html"; return false; } console.log("Admin session found."); return true; } catch (error) { console.error( "Admin initialization error:", error ); showFatalError( error.message || "Unable to initialize admin panel." ); return false; } } // ========================================================= // CREATE DASHBOARD // ========================================================= function createDashboard() { document.body.innerHTML = ` <div class="adminWrapper"> <div class="adminHeader"> <div> <h1>Travel With Sri Lanka</h1> <p>Website Content Management</p> </div> <button id="logoutBtn" class="logoutBtn" > Logout </button> </div> <div id="message" class="message" ></div> <!-- DESTINATIONS --> <section class="adminSection"> <div class="sectionHeader"> <div> <h2>Destinations</h2> <p> Manage destination information and photos. </p> </div> <button id="addDestinationBtn" class="primaryBtn" > + Add Destination </button> </div> <div id="destinationsList" class="contentGrid" > <p>Loading destinations...</p> </div> </section> <!-- TOURS --> <section class="adminSection"> <div class="sectionHeader"> <div> <h2>Tours</h2> <p> Manage tour information and photos. </p> </div> <button id="addTourBtn" class="primaryBtn" > + Add Tour </button> </div> <div id="toursList" class="contentGrid" > <p>Loading tours...</p> </div> </section> <!-- MODAL --> <div id="contentModal" class="modal" > <div class="modalBox"> <button id="closeModal" class="closeBtn" > × </button> <h2 id="modalTitle"> Edit Content </h2> <form id="contentForm"> <input type="hidden" id="contentId" > <input type="hidden" id="contentType" > <label> Name / Title </label> <input type="text" id="contentName" required > <label> Slug </label> <input type="text" id="contentSlug" required > <label> Description </label> <textarea id="contentDescription" rows="5" ></textarea> <label> Page URL </label> <input type="text" id="contentPageUrl" placeholder="ella.html" > <label> Current Image </label> <div id="currentImage" class="currentImage" > <p>No image selected.</p> </div> <label> Upload New Image </label> <input type="file" id="contentImage" accept="image/*" > <label> Sort Order </label> <input type="number" id="contentSortOrder" value="1" min="0" > <button type="submit" class="saveBtn" > Save Changes </button> </form> </div> </div> </div> <style> * { box-sizing: border-box; } body { margin: 0; font-family: Arial, Helvetica, sans-serif; background: #f4f7f5; color: #17221d; } .adminWrapper { max-width: 1200px; margin: auto; padding: 30px 20px 80px; } .adminHeader { display: flex; justify-content: space-between; align-items: center; gap: 20px; background: white; padding: 25px; border-radius: 16px; box-shadow: 0 5px 25px rgba(0,0,0,.07); margin-bottom: 25px; } .adminHeader h1 { margin: 0 0 5px; } .adminHeader p { margin: 0; color: #68736d; } button { border: none; cursor: pointer; font: inherit; } .logoutBtn { background: #222; color: white; padding: 11px 20px; border-radius: 8px; } .adminSection { background: white; padding: 25px; border-radius: 16px; margin-bottom: 30px; box-shadow: 0 5px 25px rgba(0,0,0,.06); } .sectionHeader { display: flex; justify-content: space-between; align-items: center; gap: 20px; margin-bottom: 25px; } .sectionHeader h2 { margin: 0 0 5px; } .sectionHeader p { margin: 0; color: #68736d; } .primaryBtn { background: #176b4d; color: white; padding: 12px 18px; border-radius: 8px; white-space: nowrap; } .contentGrid { display: grid; grid-template-columns: repeat( auto-fit, minmax(280px, 1fr) ); gap: 20px; } .contentCard { border: 1px solid #e1e7e3; border-radius: 12px; overflow: hidden; background: #fff; } .contentImage { width: 100%; height: 190px; object-fit: cover; background: #e8ecea; } .contentBody { padding: 18px; } .contentBody h3 { margin: 0 0 8px; } .contentBody p { color: #68736d; font-size: 14px; line-height: 1.6; } .cardActions { display: flex; gap: 10px; margin-top: 15px; } .editBtn, .deleteBtn { flex: 1; color: white; padding: 10px; border-radius: 7px; } .editBtn { background: #176b4d; } .deleteBtn { background: #c0392b; } .message { display: none; padding: 13px 16px; margin-bottom: 20px; border-radius: 8px; background: #e8f5ee; } .modal { display: none; position: fixed; inset: 0; background: rgba(0,0,0,.55); z-index: 9999; padding: 20px; overflow-y: auto; } .modalBox { position: relative; max-width: 600px; margin: 40px auto; background: white; padding: 30px; border-radius: 15px; } .closeBtn { position: absolute; right: 15px; top: 10px; width: 35px; height: 35px; border-radius: 50%; background: #eee; font-size: 25px; } .modalBox h2 { margin-top: 0; } .modalBox label { display: block; margin: 18px 0 7px; font-weight: bold; } .modalBox input, .modalBox textarea { width: 100%; padding: 12px; border: 1px solid #d8dfdb; border-radius: 7px; font: inherit; } .modalBox textarea { resize: vertical; } .currentImage img { width: 100%; max-height: 220px; object-fit: cover; border-radius: 8px; } .saveBtn { width: 100%; margin-top: 25px; padding: 14px; background: #176b4d; color: white; border-radius: 8px; font-weight: bold; } .saveBtn:disabled { opacity: .6; cursor: not-allowed; } @media (max-width: 600px) { .adminWrapper { padding: 15px 10px 50px; } .adminHeader { flex-direction: column; align-items: stretch; } .sectionHeader { flex-direction: column; align-items: stretch; } .primaryBtn { width: 100%; } .adminSection { padding: 18px; } } </style> `; setupDashboardEvents(); } // ========================================================= // MESSAGE // ========================================================= function showMessage(text) { const message = document.getElementById("message"); if (!message) return; message.textContent = text; message.style.display = "block"; setTimeout(() => { message.style.display = "none"; }, 3000); } // ========================================================= // LOAD DESTINATIONS // ========================================================= async function loadDestinations() { const container = document.getElementById("destinationsList"); if (!container) return; container.innerHTML = "<p>Loading destinations...</p>"; try { const { data, error } = await supabase .from("destinations") .select("*") .order("sort_order", { ascending: true }); if (error) { throw error; } if (!data || !data.length) { container.innerHTML = "<p>No destinations found.</p>"; return; } container.innerHTML = ""; data.forEach(destination => { const card = document.createElement("div"); card.className = "contentCard"; let imageHTML = `<div class="contentImage"></div>`; if (destination.image_url) { imageHTML = ` <img src="${escapeHTML(destination.image_url)}" class="contentImage" alt="${escapeHTML(destination.name)}" > `; } card.innerHTML = ` ${imageHTML} <div class="contentBody"> <h3> ${escapeHTML(destination.name)} </h3> <p> ${escapeHTML( destination.description || "" )} </p> <div class="cardActions"> <button class="editBtn" data-id="${destination.id}" data-type="destination" > Edit </button> <button class="deleteBtn" data-id="${destination.id}" data-type="destination" > Delete </button> </div> </div> `; container.appendChild(card); }); attachCardButtons(); } catch (error) { console.error(error); container.innerHTML = ` <p> Failed to load destinations. <br><br> ${escapeHTML(error.message)} </p> `; } } // ========================================================= // LOAD TOURS // ========================================================= async function loadTours() { const container = document.getElementById("toursList"); if (!container) return; container.innerHTML = "<p>Loading tours...</p>"; try { const { data, error } = await supabase .from("tours") .select("*") .order("sort_order", { ascending: true }); if (error) { throw error; } if (!data || !data.length) { container.innerHTML = "<p>No tours found.</p>"; return; } container.innerHTML = ""; data.forEach(tour => { const card = document.createElement("div"); card.className = "contentCard"; let imageHTML = `<div class="contentImage"></div>`; if (tour.image_url) { imageHTML = ` <img src="${escapeHTML(tour.image_url)}" class="contentImage" alt="${escapeHTML(tour.title)}" > `; } card.innerHTML = ` ${imageHTML} <div class="contentBody"> <h3> ${escapeHTML(tour.title)} </h3> <p> ${escapeHTML( tour.description || "" )} </p> <div class="cardActions"> <button class="editBtn" data-id="${tour.id}" data-type="tour" > Edit </button> <button class="deleteBtn" data-id="${tour.id}" data-type="tour" > Delete </button> </div> </div> `; container.appendChild(card); }); attachCardButtons(); } catch (error) { console.error(error); container.innerHTML = ` <p> Failed to load tours. <br><br> ${escapeHTML(error.message)} </p> `; } } // ========================================================= // CARD BUTTONS // ========================================================= function attachCardButtons() { document .querySelectorAll(".editBtn") .forEach(button => { button.addEventListener( "click", () => { openEditModal( button.dataset.id, button.dataset.type ); } ); }); document .querySelectorAll(".deleteBtn") .forEach(button => { button.addEventListener( "click", () => { deleteContent( button.dataset.id, button.dataset.type ); } ); }); } // ========================================================= // OPEN ADD MODAL // ========================================================= function openAddModal(type) { document .getElementById("contentModal") .style.display = "block"; document .getElementById("modalTitle") .textContent = type === "destination" ? "Add Destination" : "Add Tour"; document .getElementById("contentId") .value = ""; document .getElementById("contentType") .value = type; document .getElementById("contentName") .value = ""; document .getElementById("contentSlug") .value = ""; document .getElementById("contentDescription") .value = ""; document .getElementById("contentPageUrl") .value = ""; document .getElementById("contentSortOrder") .value = "1"; document .getElementById("contentImage") .value = ""; document .getElementById("currentImage") .innerHTML = "<p>No image selected.</p>"; } // ========================================================= // OPEN EDIT MODAL // ========================================================= async function openEditModal(id, type) { const table = type === "destination" ? "destinations" : "tours"; const { data, error } = await supabase .from(table) .select("*") .eq("id", id) .single(); if (error) { console.error(error); alert( "Failed to load content:\n" + error.message ); return; } document .getElementById("contentModal") .style.display = "block"; document .getElementById("modalTitle") .textContent = type === "destination" ? "Edit Destination" : "Edit Tour"; document .getElementById("contentId") .value = data.id; document .getElementById("contentType") .value = type; document .getElementById("contentName") .value = type === "destination" ? data.name : data.title; document .getElementById("contentSlug") .value = data.slug; document .getElementById("contentDescription") .value = data.description || ""; document .getElementById("contentPageUrl") .value = data.page_url || ""; document .getElementById("contentSortOrder") .value = data.sort_order || 0; document .getElementById("contentImage") .value = ""; const currentImage = document.getElementById( "currentImage" ); if (data.image_url) { currentImage.innerHTML = ` <img src="${escapeHTML(data.image_url)}" alt="Current image" > `; } else { currentImage.innerHTML = "<p>No image uploaded.</p>"; } } // ========================================================= // CLOSE MODAL // ========================================================= function closeModal() { document .getElementById("contentModal") .style.display = "none"; } // ========================================================= // UPLOAD IMAGE // ========================================================= async function uploadImage( file, type, slug ) { if (!file) { return null; } const extension = file.name .split(".") .pop() .toLowerCase(); const safeSlug = slug .toLowerCase() .replace( /[^a-z0-9-]/g, "-" ); const fileName = `${safeSlug}-${Date.now()}.${extension}`; const folder = type === "destination" ? `destinations/${safeSlug}` : `tours/${safeSlug}`; const filePath = `${folder}/${fileName}`; const { error } = await supabase .storage .from("site-images") .upload( filePath, file, { cacheControl: "3600", upsert: false, contentType: file.type } ); if (error) { console.error(error); throw new Error( "Image upload failed: " + error.message ); } const { data } = supabase .storage .from("site-images") .getPublicUrl(filePath); return data.publicUrl; } // ========================================================= // SAVE CONTENT // ========================================================= async function saveContent(event) { event.preventDefault(); const id = document .getElementById("contentId") .value; const type = document .getElementById("contentType") .value; const name = document .getElementById("contentName") .value .trim(); const slug = document .getElementById("contentSlug") .value .trim(); const description = document .getElementById("contentDescription") .value .trim(); const pageUrl = document .getElementById("contentPageUrl") .value .trim(); const sortOrder = Number( document .getElementById("contentSortOrder") .value ); const file = document .getElementById("contentImage") .files[0]; const saveBtn = document.querySelector(".saveBtn"); saveBtn.disabled = true; saveBtn.textContent = "Saving..."; try { let imageUrl = null; if (file) { imageUrl = await uploadImage( file, type, slug ); } const table = type === "destination" ? "destinations" : "tours"; const dataToSave = type === "destination" ? { name: name, slug: slug, description: description, page_url: pageUrl, sort_order: sortOrder } : { title: name, slug: slug, description: description, sort_order: sortOrder }; if (imageUrl) { dataToSave.image_url = imageUrl; } if (id) { const { error } = await supabase .from(table) .update({ ...dataToSave, updated_at: new Date().toISOString() }) .eq("id", id); if (error) { throw error; } showMessage( "Content updated successfully." ); } else { const { error } = await supabase .from(table) .insert(dataToSave); if (error) { throw error; } showMessage( "Content added successfully." ); } closeModal(); await loadDestinations(); await loadTours(); } catch (error) { console.error(error); alert( "Save failed:\n" + error.message ); } finally { saveBtn.disabled = false; saveBtn.textContent = "Save Changes"; } } // ========================================================= // DELETE CONTENT // ========================================================= async function deleteContent( id, type ) { const confirmed = confirm( "Are you sure you want to delete this content?" ); if (!confirmed) { return; } const table = type === "destination" ? "destinations" : "tours"; const { error } = await supabase .from(table) .delete() .eq("id", id); if (error) { console.error(error); alert( "Delete failed:\n" + error.message ); return; } showMessage( "Content deleted successfully." ); await loadDestinations(); await loadTours(); } // ========================================================= // DASHBOARD EVENTS // ========================================================= function setupDashboardEvents() { document .getElementById("closeModal") .addEventListener( "click", closeModal ); document .getElementById("contentForm") .addEventListener( "submit", saveContent ); document .getElementById("addDestinationBtn") .addEventListener( "click", () => { openAddModal("destination"); } ); document .getElementById("addTourBtn") .addEventListener( "click", () => { openAddModal("tour"); } ); document .getElementById("logoutBtn") .addEventListener( "click", async () => { await supabase.auth.signOut(); window.location.href = "admin.html"; } ); window.addEventListener( "click", event => { const modal = document.getElementById( "contentModal" ); if ( event.target === modal ) { closeModal(); } } ); } // ========================================================= // START ADMIN // ========================================================= async function startAdmin() { try { console.log( "Starting admin dashboard..." ); const loggedIn = await checkLogin(); if (!loggedIn) { return; } console.log( "Creating dashboard..." ); createDashboard(); await Promise.all([ loadDestinations(), loadTours() ]); console.log( "Admin dashboard loaded." ); } catch (error) { console.error( "START ADMIN ERROR:", error ); showFatalError( error.message || "Unknown admin panel error." ); } } startAdmin();
