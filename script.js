/* ========================================================= TRAVEL WITH SRI LANKA Main JavaScript + Supabase Reviews ========================================================= */ /* ========================================================= SUPABASE ========================================================= */ import { createClient } from "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm"; const SUPABASE_URL = "https://vbbmnzqrvoceqbwwwsrc.supabase.co"; const SUPABASE_ANON_KEY = "sb_publishable_HZ1A8CURkRFs0v21FUT0VA_44dtPzr3"; const supabase = createClient( SUPABASE_URL, SUPABASE_ANON_KEY ); /* ========================================================= MOBILE MENU ========================================================= */ function toggleMenu() { const nav = document.getElementById("mainNav"); if (nav) { nav.classList.toggle("open"); } } const menuButton = document.getElementById("menuBtn"); if (menuButton) { menuButton.addEventListener( "click", toggleMenu ); } document .querySelectorAll("#mainNav a") .forEach(function(link) { link.addEventListener( "click", function() { const nav = document.getElementById("mainNav"); if (nav) { nav.classList.remove("open"); } } ); }); /* ========================================================= WHATSAPP TRIP FORM ========================================================= */ const tripForm = document.getElementById("tripForm"); if (tripForm) { tripForm.addEventListener( "submit", function(event) { event.preventDefault(); const name = document .getElementById("name") .value .trim(); const country = document .getElementById("country") .value .trim(); const date = document .getElementById("date") .value; const guests = document .getElementById("guests") .value; const message = document .getElementById("message") .value .trim(); const text = "Hello Travel With Sri Lanka!" + "\n\n" + "I would like to plan a Sri Lanka trip." + "\n\n" + "Name: " + name + "\n" + "Country: " + country + "\n" + "Travel Date: " + (date || "Not specified") + "\n" + "Guests: " + guests + "\n\n" + "Travel Preferences:" + "\n" + (message || "Not specified"); const whatsappURL = "https://wa.me/94758453391?text=" + encodeURIComponent(text); window.open( whatsappURL, "_blank" ); } ); } /* ========================================================= REVIEW VARIABLES ========================================================= */ let selectedRating = 0; /* ========================================================= STAR RATING ========================================================= */ const stars = document.querySelectorAll( "#starRating button" ); const ratingInput = document.getElementById( "reviewRating" ); stars.forEach(function(star) { star.addEventListener( "click", function() { selectedRating = Number( this.dataset.rating ); if (ratingInput) { ratingInput.value = selectedRating; } stars.forEach(function(s) { const value = Number( s.dataset.rating ); s.classList.toggle( "selected", value <= selectedRating ); }); } ); }); /* ========================================================= PHOTO PREVIEW ========================================================= */ const photoInput = document.getElementById( "reviewPhoto" ); const photoPreview = document.getElementById( "photoPreview" ); if (photoInput) { photoInput.addEventListener( "change", function() { if (photoPreview) { photoPreview.innerHTML = ""; } const file = this.files[0]; if (!file) { return; } const allowedTypes = [ "image/jpeg", "image/png", "image/webp" ]; if ( !allowedTypes.includes( file.type ) ) { this.value = ""; alert( "Please select a JPG, PNG or WebP image." ); return; } if ( file.size > 5 * 1024 * 1024 ) { this.value = ""; alert( "Image must be smaller than 5MB." ); return; } const image = document.createElement( "img" ); image.src = URL.createObjectURL( file ); image.className = "reviewPreview"; if (photoPreview) { photoPreview.appendChild( image ); } } ); } /* ========================================================= REVIEW FORM ========================================================= */ const reviewForm = document.getElementById( "reviewForm" ); if (reviewForm) { reviewForm.addEventListener( "submit", async function(event) { event.preventDefault(); const status = document.getElementById( "reviewStatus" ); const submitButton = document.getElementById( "reviewSubmit" ); const name = document .getElementById( "reviewName" ) .value .trim(); const country = document .getElementById( "reviewCountry" ) .value .trim(); const review = document .getElementById( "reviewText" ) .value .trim(); const rating = Number( document.getElementById( "reviewRating" ).value ); const file = document.getElementById( "reviewPhoto" ).files[0]; if (rating < 1) { status.textContent = "Please select a star rating."; status.className = "reviewStatus error"; return; } if (!name || !country || !review) { status.textContent = "Please complete all required fields."; status.className = "reviewStatus error"; return; } if (file) { const allowedTypes = [ "image/jpeg", "image/png", "image/webp" ]; if ( !allowedTypes.includes( file.type ) ) { status.textContent = "Please upload a JPG, PNG or WebP image."; status.className = "reviewStatus error"; return; } if ( file.size > 5 * 1024 * 1024 ) { status.textContent = "Photo must be smaller than 5MB."; status.className = "reviewStatus error"; return; } } submitButton.disabled = true; submitButton.textContent = "Posting Review..."; status.textContent = "Please wait..."; status.className = "reviewStatus"; try { let photoURL = null; let photoPath = null; /* ================================================ UPLOAD PHOTO TO SUPABASE STORAGE ================================================ */ if (file) { const extension = file.name .split(".") .pop() .toLowerCase(); const uniqueName = Date.now() + "-" + crypto.randomUUID() + "." + extension; photoPath = uniqueName; const uploadResult = await supabase .storage .from("review-photos") .upload( photoPath, file, { cacheControl: "3600", contentType: file.type, upsert: false } ); if ( uploadResult.error ) { throw uploadResult.error; } const publicURLResult = supabase .storage .from("review-photos") .getPublicUrl( photoPath ); photoURL = publicURLResult .data .publicUrl; } /* ================================================ SAVE REVIEW TO SUPABASE ================================================ */ const insertResult = await supabase .from("reviews") .insert({ name: name, country: country, rating: rating, review: review, photo_url: photoURL }); if ( insertResult.error ) { if (photoPath) { await supabase .storage .from("review-photos") .remove([ photoPath ]); } throw insertResult.error; } /* ================================================ SUCCESS ================================================ */ status.textContent = "Your review has been posted successfully!"; status.className = "reviewStatus success"; reviewForm.reset(); selectedRating = 0; if (ratingInput) { ratingInput.value = "0"; } stars.forEach( function(star) { star.classList.remove( "selected" ); } ); if (photoPreview) { photoPreview.innerHTML = ""; } await loadReviews(); } catch (error) { console.error( "Review error:", error ); status.textContent = "Unable to post your review. Please try again."; status.className = "reviewStatus error"; } finally { submitButton.disabled = false; submitButton.textContent = "Post My Review →"; } } ); } /* ========================================================= LOAD REVIEWS ========================================================= */ async function loadReviews() { const container = document.getElementById( "reviewsContainer" ); if (!container) { return; } container.innerHTML = ` <div class="reviewsLoading"> Loading traveller reviews... </div> `; try { const result = await supabase .from("reviews") .select( "id, name, country, rating, review, photo_url, created_at" ) .order( "created_at", { ascending: false } ); if (result.error) { throw result.error; } const reviews = result.data || []; container.innerHTML = ""; if ( reviews.length === 0 ) { container.innerHTML = ` <div class="noReviews"> No traveller reviews yet. Be the first to share your experience! </div> `; return; } reviews.forEach( function(data) { const card = createReviewCard( data ); container.appendChild( card ); } ); } catch (error) { console.error( "Loading reviews failed:", error ); container.innerHTML = ` <div class="noReviews"> Reviews are temporarily unavailable. </div> `; } } /* ========================================================= CREATE REVIEW CARD ========================================================= */ function createReviewCard( data ) { const article = document.createElement( "article" ); article.className = "reviewCard realReviewCard"; const rating = Math.max( 1, Math.min( 5, Number(data.rating) || 5 ) ); const starsHTML = "★".repeat( rating ) + "☆".repeat( 5 - rating ); const name = escapeHTML( data.name || "Traveller" ); const country = escapeHTML( data.country || "" ); const text = escapeHTML( data.review || "" ); let photoHTML = ""; if (data.photo_url) { photoHTML = ` <img src="${escapeAttribute(data.photo_url)}" class="reviewPhoto" alt="Photo shared by ${name}" loading="lazy"> `; } article.innerHTML = ` ${photoHTML} <div class="rating"> ${starsHTML} </div> <p> “${text}” </p> <strong> ${name} </strong> <span class="reviewCountry"> ${country} </span> `; return article; } /* ========================================================= SECURITY HELPERS ========================================================= */ function escapeHTML( value ) { return String(value) .replace( /&/g, "&amp;" ) .replace( /</g, "&lt;" ) .replace( />/g, "&gt;" ) .replace( /"/g, "&quot;" ) .replace( /'/g, "&#039;" ); } function escapeAttribute( value ) { return escapeHTML( value ); } /* ========================================================= LOAD REVIEWS ON PAGE LOAD ========================================================= */ loadReviews();
